---
description: Deploy the full Cathedral stack (Flask server, WebSocket, monitors).
trigger: manual
command: /deploy
---

Start the Flask API server, WebSocket broadcaster, and background monitors. Verify all services are healthy. Output the dashboard URL and a Cant‑formatted readiness message.

<!-- DEPLOY: full stack -->
